// lib/vapi.ts
export async function callVapi(text: string): Promise<string> {
    // Logique pour appeler l'API VAPI/Gemini
    // par ex:
    // const resp = await fetch('https://api.vapi.ai/v1/completions', {...});
    // return ...
    return `Réponse reçue pour: ${text}`;
}
